<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เนื้อหาวีดีโอ</title>
    <meta name="keyword" content="วีดีโอข่าวอนิเมะ และตัวอย่าง PV เกี่ยวกับอนิเมะ">
    <meta name="keyword" content="แพลตฟอร์มรวบรวมข่าวและอนิเมะในวงการ และรับค่าตอบแทนจากเนื้อหาของคุณ">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/pages/home_Section.css">
    <link rel="stylesheet" href="./css/pages/uploads/video.css">
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-6579807593228261"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="./css/pages/videos.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet" />
</head>
<style>
    .pic_photo {
        position: relative;
    }

    .duration {
        position: absolute;
        bottom: 5px;
        right: 5px;
        background-color: rgba(0, 0, 0, 0.7);
        color: #fff;
        padding: 2px 10px;
        border-radius: 8px;
        font-size: 19px;
        font-family: "Kanit", sans-serif;
        font-weight: 400;
        font-style: normal;
    }
</style>

<body>
    <%- include('../../navnar_Header') %>
        <%- include('../../sidebar_Section') %>

            <section class="content">
                <div class="loading_container">
            <div class="loading-indicator loadingIndicator" id="loadingIndicator">
                <div class="spinner"></div>
            </div>
            <p>กำลังประมวณผลข้อมูล</p>
            <p>ยังไม่พร้อมให้บริการหน้านี้</p>
        </div>
                <div class="mt-8 bg-white px-6 md:px-12 py-4 shadow-md rounded-md relative" style="display: none;">
                    <h2 class="text-3xl mb-6 border-l-4 border-sky-600 px-5 text-gray-600">วีดีโอเนื้อหา</h2>
                    <div class="videos">
                        <div class="video">
                            <a href="#">
                                <div class="pic_photo">
                                    <img src="/images/demo.jpg" alt="">
                                    <video class="video" src="/videos/13024.mp4" style="display: none;"></video>
                                    <span class="time duration">9:00</span>
                                </div>
                                <div class="text_video">
                                    <h2>Lorem ipsum dolor sit amet consectetur adipisicing elit. Doloribus, impedit.
                                    </h2>
                                    <p class="views">0 วิว</p>
                                </div>
                            </a>
                        </div>

                        <div class="video">
                            <a href="#">
                                <div class="pic_photo">
                                    <img src="/images/demo.jpg" alt="">
                                    <video class="video" src="/videos/13024.mp4" style="display: none;"></video>
                                    <span class="time duration">9:00</span>
                                </div>
                                <div class="text_video">
                                    <h2>Lorem ipsum dolor sit amet consectetur adipisicing elit. Doloribus, impedit.
                                    </h2>
                                    <p class="views">0 วิว</p>
                                </div>
                            </a>
                        </div>

                    </div>
                </div>
            </section>

            <script src="./js/pages/script_Section.js"></script>
            <script>
                document.addEventListener("DOMContentLoaded", function () {
                    const videos = document.querySelectorAll('.video video');

                    videos.forEach(video => {
                        const img = video.parentElement.querySelector('img');

                        video.parentElement.addEventListener('mouseenter', () => {
                            if (video.paused) {
                                video.play();
                                video.style.display = 'block';
                                img.style.display = 'none';
                            } else {
                                video.pause();
                                img.style.display = 'block';
                                video.style.display = 'none';
                            }
                        });

                        video.addEventListener('mouseleave', () => {
                            video.pause();
                            img.style.display = 'block';
                            video.style.display = 'none';
                        });

                        video.addEventListener('contextmenu', (e) => {
                            e.preventDefault();
                        });
                    });

                    videos.forEach(video => {
                        const durationSpan = video.parentElement.querySelector('.duration');

                        if (durationSpan) {
                            video.addEventListener("canplaythrough", (e) => {
                                let videoDuration = e.target.duration;
                                let totalMin = Math.floor(videoDuration / 60);
                                let totalSec = (videoDuration % 60).toFixed(0);

                                totalSec = totalSec < 10 ? "0" + totalSec : totalSec;

                                durationSpan.textContent = `${totalMin}:${totalSec}`;
                            });
                        }
                    });
                });


                function playvideo() {
                    const play_v = document.querySelectorAll("video");
                    play_v.play();
                }
            </script>



            <!-- <script src="./js/video.js"></script>  -->
            <!-- <script>
            let currentVideo = null;

            function playVideo(videoId) {
                const video = document.getElementById(videoId);

                if (currentVideo && currentVideo !== video) {
                    currentVideo.pause();
                    currentVideo.currentTime = 0;
                }

                video.play();
                video.volume = 0.2;
                currentVideo = video;
            }

            document.querySelectorAll('.video').forEach(video => {
                video.volume = 0.3;
                video.addEventListener('mouseover', () => {
                    playVideo(video.id);
                });
            });

         // Blob URL video
    document.querySelectorAll('video').forEach(video => {
        const videoUrl = video.src;
        if (videoUrl.startsWith('blob:')) return;
        const xhr = new XMLHttpRequest();
        xhr.open('GET', videoUrl);
        xhr.responseType = 'arraybuffer';
        xhr.onload = () => {
            const blob = new Blob([xhr.response]);
            const url = URL.createObjectURL(blob);
            video.src = url;
        };
        xhr.send();
    });

        </script> -->
</body>

</html>