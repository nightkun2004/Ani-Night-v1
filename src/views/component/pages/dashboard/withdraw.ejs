<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ถอนเงิน</title>
    <link rel="shortcut icon" href="/images/logo/Aninight.png" type="image/x-icon">
    <link rel="stylesheet" href="/css/footer.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <%- include('../../../navnar_Header') %>
    <div class="mx-auto px-4 xl:max-w-7xl pt-20">
        <div class="bg-white p-8 rounded-lg shadow-lg">
            <h1 class="text-3xl font-semibold text-gray-800 mb-6">ถอนเงิน</h1>
            <a class="bg-sky-500 text-white rounded p-2" href="/withdrawal-history">ประวัติการถอนเงิน</a>
            <div class="flex justify-between">
                <p class="text-lg font-bold text-gray-600">ยอดคะแนน:</p>
                <p class="text-xl font-semibold text-gray-800"><%= user.points %> คะแนน</p>
            </div>
            <div class="mb-4">
                <p class="text-sm text-gray-600">เลือกระบบการถอนเงิน:</p>
               <div class="flex items-center">
                <img src="/icons/icon_trueMoney.png" class="w-10 h-10" alt="">
                <p>True Money Wallet</p>
               </div>
               <form action="/withdraw/payment" method="post">
                <div class="flex flex-col items-center">
                    <label class="items-center">
                        <input type="radio" class="form-radio text-blue-500" name="withdrawMethod" value="1">
                        <span class="ml-2">2000 คะแนน  0.20 บาท</span>
                    </label>
                    <label class="items-center">
                        <input type="radio" class="form-radio text-2xl text-blue-500" name="withdrawMethod" value="2">
                        <span class="ml-2">5000 คะแนน  0.50 บาท</span>
                    </label>
                    <label class="items-center">
                        <input type="radio" class="form-radio text-blue-500" name="withdrawMethod" value="3" checked>
                        <span class="ml-2">10,000 คะแนน  1.10 บาท</span>
                    </label>
                    <label class="items-center">
                        <input type="radio" class="form-radio text-blue-500" name="withdrawMethod" value="4" checked>
                        <span class="ml-2">50,000 คะแนน  5.00 บาท</span>
                    </label>
                </div>
            
                <button type="submit" class="w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded">
                    ถอนเงิน
                </button>

                <% if (errorMessage) {%>
                    <div class="bg-red-600 rounded-lg p-2 mb-3 mt-3">
                    <p class="text-1xl font-bold text-white"><%= errorMessage %></p>
                   </div>
                <% } %>
                <% if (messagess) {%>
                    <div class="bg-green-600 rounded-lg p-2 mb-3 mt-3">
                    <p class="text-1xl font-bold text-white"><%= messagess %></p>
                   </div>
                <% } %>
            </form>
            
            </div>
        </div>
    </div>
</body>
</html>