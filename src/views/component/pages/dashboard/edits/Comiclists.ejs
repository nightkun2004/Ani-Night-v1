<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>คอมมิคทั้งหมด</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/df26bd9708.js" crossorigin="anonymous"></script>
</head>
<body>
    <div class="flex justify-between">
        <%- include('../../../../admin/sidebars/navbar') %>
        <div class="main-section w-full min-h-screen">
            <div class="p-2">
                <h1 class="text-3xl font-bold mb-6 text-center">รายการคอมมิคทั้งหมด</h1>
            <div class="flex justify-end mb-4">
                <a href="/admin/create/comic" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">เพิ่มคอมมิคใหม่</a>
            </div>
            <% if(Comiclists.length > 0) { %>
                <div class="flex flex-wrap">
                    <% Comiclists.forEach(comic => { %>
                        <div class="w-full md:w-1/3 xl:w-1/4 p-6 flex flex-col">
                            <a href="/admin/Edit/chapter?comicId=<%= comic._id %>">
                                <img class="hover:grow rounded-lg hover:shadow-lg" src="/posters/<%= comic.poster %>" alt="comic">
                                <div class="pt-3 flex items-center justify-between">
                                    <p class="text-1xl font-bold">
                                        <%= comic.titlecomic %>
                                    </p>
                                    <a href="/admin/Edit/chapter?comicId=<%= comic._id %>" class="block bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded text-center mb-2">เพิ่มตอน</a>
                                </div>
                            </a>
                        </div>
                   
                    <% }) %>
                </div>
                
            <% } else { %>
                <p class="text-center">ไม่พบคอมมิค</p>
            <% } %>
            </div>
        </div>
</body>
</html>