<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= userData.username %>
    </title>
    <link rel="stylesheet" href="../css/pages/home_Section.css">
    <link rel="stylesheet" href="../css/channel.css">
    <link rel="stylesheet" href="../css/font/font1.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.9.0/slick.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.9.0/slick-theme.min.css">
    <meta name="keywords" content="บทความอนิเมะ ข่าวบันเท้ง 2024 login เข้าสู่ระบบ">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <%- include('../navnar_Header') %>

    <section class="editor mx-auto px-4 xl:max-w-7xl pt-[110px]" id="editor">

            <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
                <div class="bg-white shadow-md rounded-lg overflow-hidden p-6">
                    <div class="flex flex-col sm:flex-row items-center">
                        <img class="h-24 w-24 rounded-full object-cover" src="/profiles/<%= userData.profile %>" alt="Profile Picture">
                        <div class="mt-4 sm:mt-0 sm:ml-6 text-center sm:text-left">
                            <h1 class="text-2xl font-bold text-gray-900">
                                <%= userData.name %>
                            </h1>
                            <p class="text-sm text-gray-600">@<%= userData.username %>
                            </p>
                            <p class="text-sm text-gray-600">
                                <%= userData.bio %>
                            </p>
                         
                            
                           
                            <div class="mt-4 flex justify-center sm:justify-start space-x-4">
                            <span class="text-sm text-gray-600"><strong><%= userData.followers %>:</strong> ผู้ติดตาม</span>
                            <span class="text-sm text-gray-600"><strong><%= userData.followed %></strong> กำลังติดตาม</span>
                            <!-- <span class="text-sm text-gray-600"><strong>0</strong> Likes</span> -->
                        </div>
                        <!-- <button class="mt-4 bg-indigo-600 text-white px-4 py-2 rounded-md">Follow</button> -->

                        </div>
                    </div>
                </div>
            </div>
        <!-- ============================================
        Menus Editor ==================================== -->
        <div
            class=" text-sm font-medium text-center text-gray-500 border-b border-gray-200 dark:text-gray-400 dark:border-gray-700">
            <ul class="navbar_profile flex flex-wrap -mb-px">
                <li class="me-2 categoryButton">
                    <a href="/editor/<%= userData.username %>"
                        class="inline-block p-4 border-b-2 text-sky-500 border-b-2 border-sky-500 rounded-t-lg hover:text-gray-600 hover:border-gray-700 dark:hover:text-gray-700">หน้าหลัก</a>
                </li>
                <li class="me-2 categoryButton">
                    <a href="/editor/<%= userData.username %>/video"
                        class="inline-block p-4 border-b-2 border-transparent rounded-t-lg hover:text-gray-600 hover:border-gray-700 dark:hover:text-gray-700"
                        aria-current="page">วีดีโอ</a>
                </li>
            </ul>
        </div>

        <!-- ===================================
            content Editor ========================= -->
        <div class="homes mb-20">
            <h2 class="text-3xl mb-6 mt-5 border-l-4 border-sky-600 px-5 text-gray-600">หน้าแรก</h2>
            <div id="video-container">
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                    <!-- Blog Post 1 -->
                    <% if (userData.acticles.length> 0) { %>
                        <% userData.acticles.forEach(item=> { %>
                    <div class="bg-white shadow-lg rounded-lg overflow-hidden">
                        <a href="/read/<%= item.url %>">
                            <img class="w-full h-48 object-cover" src="/acticles_images/<%= item.photo %>" alt="Post Image">
                            <div class="p-6">
                                <h2 class="text-2xl font-bold mb-2 text-gray-800"> <%= item.title %></h2>
                                <p class="text-gray-600">   <%= item.views %> วิว</p>
                            </div>
                        </a>
                    </div>
                    <% }); %>
                    <% } else { %>
                        <p style="text-align: center;">ครีเอเตอร์นี้ยังไม่มีบทความครับ..</p>
                     <% } %>
                    <!-- More blog posts can be added here in the same format -->
                </div>
                </div>

        </div>
    </section>

    <script>
        window.addEventListener('scroll', function () {
            var navbar = document.getElementById('navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('navbar-white-bg');
            } else {
                navbar.classList.remove('navbar-white-bg');
            }
        });

    </script>
</body>

</html>