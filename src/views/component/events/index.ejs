<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>อีเว้นท์กิจกรรมผู้เข้าร่วม</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <%- include('../../navnar_Header') %>
    <%- include('../../sidebar_Section') %>

    <div class="container mx-auto">
        <h3 class="text-3xl">หน้านี้ยังอยู่ระหว่างพัฒนา</h3>
        <div class="bg-white shadow-md rounded-lg">
            <ul>
                <% if (activities.length === 0) { %>
                    <li class="py-4 px-4 text-center text-gray-500">No activitiess available</li>
                <% } else { %>
                    <% activities.forEach(activitie => { %>
                        <li class="py-4 px-4 border-b border-gray-200 mb-5">
                            <div class="mb-4">
                                <img src="<%= activitie.imagePath %>" alt="Image" class="w-full h-64 object-cover rounded-lg">
                            </div>
                            <div class="flex-1 mb-2">
                                <p class="text-sm">
                                    <span class="font-bold"><%= activitie.title %></span>
                                    <%= activitie.description %>
                                </p>
                                <p class="text-xs text-gray-500"><%= activitie.createdAt.toDateString() %></p>
                            </div>
                            <a href="/event/<%= activitie.linkurlparams %>" class="text-blue-500">ดูรายละเอียด</a>
                        </li>
                    <% }) %>
                <% } %>
            </ul>
        </div>
    </div>
    <%- include('../../footer') %>
</body>
</html>