<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Activity</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

    <%- include('../sidebars/navbar') %>
    <div class="main-section w-full min-h-screen">
        <div class="bg-white p-6">
            <h2 class="text-2xl font-bold mb-6">สร้างหน้ากิจกรรม</h2>

            <% if (query.success) { %>
                <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-4">
                    <p>Upload successful!</p>
                </div>
            <% } %>

            <% if (query.error) { %>
                <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4">
                    <p>Upload failed!</p>
                </div>
            <% } %>

            <form action="/add/Activity/upload/html" method="POST" enctype="multipart/form-data">
                <div class="mb-4">
                    <label for="title" class="block text-gray-700">Title:</label>
                    <input type="text" id="title" name="title" class="w-full px-3 py-2 border rounded">
                </div>
                <div class="mb-4">
                    <label for="description" class="block text-gray-700">Description:</label>
                    <textarea id="description" name="description" class="w-full px-3 py-2 border rounded"></textarea>
                </div>
                <div class="mb-4">
                    <label for="htmlFile" class="block text-gray-700">HTML File:</label>
                    <input type="file" id="htmlFile" name="htmlFile" class="w-full px-3 py-2 border rounded">
                </div>
                <div class="mb-4">
                    <label for="cssCdn" class="block text-gray-700">CSS CDN:</label>
                    <input type="text" id="cssCdn" name="cssCdn" class="w-full px-3 py-2 border rounded">
                </div>
                <div class="mb-4">
                    <label for="jsCdn" class="block text-gray-700">JS CDN:</label>
                    <input type="text" id="jsCdn" name="jsCdn" class="w-full px-3 py-2 border rounded">
                </div>
                <div class="mb-4">
                    <label for="jsCdn" class="block text-gray-700">URL :</label>
                    <input type="text" id="linkurlparams" name="linkurlparams" class="w-full px-3 py-2 border rounded">
                </div>
                <div class="mb-4">
                    <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Upload</button>
                </div>
            </form>
        </div>
    </div>

</body>
</html>
