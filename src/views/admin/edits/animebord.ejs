<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>สร้างอนิเมะบอร์ด</title>
    <link rel="stylesheet" href="../css/admin/dash.css">
    <link rel="stylesheet" href="../css/admin/createAnime.css">
    <link rel="stylesheet" href="../css/admin/sidebar.css">
    <link rel="stylesheet" href="../css/footer.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="../css/acticle.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/df26bd9708.js" crossorigin="anonymous"></script>
</head>
<body>
    <div class="fixed inset-x-0 z-20 md:none" id="navbar">
        <%- include('../../admin/sidebars/navbar') %>
    </div>
    <div class="col_form mx-auto px-4 xl:max-w-7xl pt-[110px]">
        <% if (usersesstion) { %>
            <h5>ดำเนินด้วยบัญชี</h5>
            <div class="profile-info">
            <img src="/profiles/<%= usersesstion.profile %>" alt="Profile Image">
            <div>
              <p><%= usersesstion.username %></p>
              <p><%= usersesstion.email %></p>
            </div>
          </div>
        <% } %>
    <div class="edit_overee">
        <form action="/edit_animeboard/one" method="POST">
            <h2>แก้ไขอนิเมะบอร์ด</h2>

            <input type="text" name="update_id" value="<%= animeApril._id %>">

            <label for="name">ชื่ออนิเมะ</label>
            <input type="text" name="nameAnime" value="<%= animeApril.nameAnime %>">

            <label for="ds">ผลิตโดย</label>
            <input type="text" name="Produced" value="<%= animeApril.Produced %>">

            <label for="on">ต้นฉบับ</label>
            <input type="text" name="manuscript" value="<%= animeApril.manuscript %>">

            <label for="ep">จำนวนตอน</label>
            <input type="text" name="episodes" value="<%= animeApril.episodes %>">

            <label for="name">เริ่มฉาย</label>
            <input type="text" name="start" value="<%= animeApril.start %>">

            <label for="link">ลิงภาพ</label>
            <input type="text" name="linkImage" value="<%= animeApril.linkImage %>">

            <% if (animeApril.info) { %>
            <label for="link">ข้อมูลเพิ่มเติม</label>
            <input type="text" name="info" value="<%= animeApril.info %>">
            <% } else { %>
                <label for="link">ข้อมูลเพิ่มเติม</label>
                <input type="text" name="info">
            <% } %>

            <% if (animeApril.web) { %>
                <label for="link">เว็บทางการ</label>
                <input type="text" name="web" value="<%= animeApril.web %>">
                <% } else { %>
                    <label for="link">เว็บทางการ</label>
                    <input type="text" name="web">
                <% } %>

            <% if (animeApril.bilibili) { %>
                <label for="link">แพลตฟอร์ม Bilibili</label>
                <input type="text" name="bilibili" value="<%= animeApril.bilibili %>">
                <% } else { %>
                    <label for="link">แพลตฟอร์ม Bilibili</label>
                    <input type="text" name="bilibili">
                <% } %>

            <input type="submit" value="เผยแพร่">
        </form>
    </div>
        
    </div>


    <%- include('../../footer') %>
</body>
</html>