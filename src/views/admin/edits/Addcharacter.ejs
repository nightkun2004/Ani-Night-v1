<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>สร้างอนิเมะบอร์ด</title>
    <link rel="stylesheet" href="../css/admin/dash.css">
    <link rel="stylesheet" href="../css/admin/createAnime.css">
    <link rel="stylesheet" href="../css/admin/Admin_section.css">
    <link rel="stylesheet" href="../css/admin/sidebar.css">
    <link rel="stylesheet" href="../css/footer.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/df26bd9708.js" crossorigin="anonymous"></script>
</head>
<body>
    <%- include('../../admin/sidebars/navbar') %>
    <section class="main-section">
        <div class="col_form mx-auto px-4 xl:max-w-7xl pt-[110px]">
            <% if (usersesstion) { %>
                <h5>ดำเนินด้วยบัญชี</h5>
                <div class="flex items-center">
                <img src="/profiles/<%= usersesstion.profile %>" alt="Profile Image" class="w-10 h-10 rounded-full">
                <div class="pl-5">
                  <p><%= usersesstion.username %></p>
                  <p><%= usersesstion.email %></p>
                </div>
              </div>
            <% } %>
            <section>
                <div class="flex justify-between">
                    <div class="flex flex-col w-full md:flex-row md:w-1/3 md:mt-5">
                        <ul class="w-full">
                            <li class="border-l-4 border-sky-600 border-right mb-5" >
                                <a href="#">
                                    แก้ไขพื้นฐาน
                                </a>
                            </li>
                           <li class="mb-5">
                            <form action="/edit/character/anime" method="post">
                                <input type="hidden" name="edit_id" value="<%= EditAnime._id %>">
                                <button type="submit">เพิ่มตัวละคร</button>
                            </form>
                           </li>
                        </ul>
                    </div>

                    <div class="w-full md-w-3/6">
                        <form action="/edit_Anime/character/add" method="POST" class="max-w-lg mx-auto p-8 bg-white shadow-lg rounded-lg">
                            <input type="text" name="animeId" value="<%= EditAnime._id %>" class="w-full px-3 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-sky-500">
                            
                            <div class="mb-4">
                                <label for="dubType" class="block text-gray-700 text-sm font-bold mb-2">เลือกรูปแบบการบันทีก Dub Type:</label>
                                <select name="dubType" id="dubType" class="w-full px-3 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-sky-500">
                                    <option value="dubjpan">Japanese Dub</option>
                                    <option value="dubthai">Thai Dub</option>
                                </select>
                            </div>
                        
                            <div class="mb-4">
                                <label for="namevoice" class="block text-gray-700 text-sm font-bold mb-2">ชื่อนักพากย์:</label>
                                <input type="text" name="namevoice" id="namevoice" class="w-full px-3 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-sky-500">
                            </div>
                            
                            <div class="mb-4">
                                <label for="voiceimage" class="block text-gray-700 text-sm font-bold mb-2">รูปภาพนักพากย์:</label>
                                <input type="text" name="voiceimage" id="voiceimage" class="w-full px-3 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-sky-500">
                            </div>
                            
                            <div class="mb-4">
                                <label for="character" class="block text-gray-700 text-sm font-bold mb-2">ตัวละคร:</label>
                                <input type="text" name="character" id="character" class="w-full px-3 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-sky-500">
                            </div>
                            
                            <div class="mb-4">
                                <label for="role" class="block text-gray-700 text-sm font-bold mb-2">บทบาท:</label>
                                <input type="text" name="role" id="role" class="w-full px-3 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-sky-500">
                            </div>
                            
                            <div class="mb-4">
                                <label for="characterimage" class="block text-gray-700 text-sm font-bold mb-2">รูปภาพตัวละคร:</label>
                                <input type="text" name="characterimage" id="characterimage" class="w-full px-3 py-2 border rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-sky-500">
                            </div>
                            
                            <div class="text-center">
                                <input type="submit" value="เพิ่มตัวละคร" class="px-4 py-2 bg-sky-500 text-white rounded-lg shadow-md hover:bg-sky-700 focus:outline-none focus:ring-2 focus:ring-sky-500 focus:ring-opacity-50">
                            </div>
                        </form>
                        
                    </div>
                </div>
            </section>
            
        </div>
    </section>
    

    <script src="/js/admins/script.js"></script>
    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
    <script>
        var quill = new Quill('#editor', {
            theme: 'snow',
            modules: {
                toolbar: [
                    ['bold', 'italic', 'underline', 'strike'],
                    ['link', 'video'],
                    [{ 'color': [] }],
                    [{ 'list': 'ordered' }, { 'list': 'bullet' }],
                    [{ 'indent': '-1' }, { 'indent': '+1' }],
                    ['align', 'script', 'code-block'],
                    [{ 'header': [1, 2, 3, 4, 5, 6] }],
                    ['clean']
                ]
            },
            formats: [
                'bold', 'italic', 'header', 'underline', 'strike', 'link', 'color', 'list', 'bullet', 'indent',
                'video', 'align', 'script', 'code-block'
            ]
        });
    
        var contentInput = document.getElementById('Synopsis');
        
        // ตั้งค่าเนื้อหาเริ่มต้นใน Quill editor
        quill.clipboard.dangerouslyPasteHTML(contentInput.value);
        
        quill.on('text-change', function() {
            contentInput.value = quill.root.innerHTML;
        });
    
        quill.getModule('toolbar').addHandler('video', function() {
            var url = prompt('Enter the URL of the video:');
            if (url) {
                insertVideo(url);
            }
        });
    
        function insertVideo(url) {
            var range = quill.getSelection();
            quill.insertEmbed(range.index, 'video', url, Quill.sources.USER);
        }
    </script>
</body>
</html>